#!/bin/bash
# ============================================================
# RÉCUPÉO — Deploy rapide (build + restart)
# Utilise le build asynchrone puis vérifie le statut
# ============================================================
cd /var/www/recupeo
./scripts/build.sh
